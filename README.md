# Matter-Controller

A starting point for a Node.js project that will serve as a controller within the Matter ecosystem.

## Getting Started

1. Clone this repository.
2. Install dependencies using `npm install`.
3. Start the application with `npm start`.